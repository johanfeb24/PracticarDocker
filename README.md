# Password Generator API (FastAPI)

Proyecto sencillo para aprender:
- Python
- FastAPI
- Docker (siguiente paso)

## Ejecutar local

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

Abrir:
- http://localhost:8000/docs
